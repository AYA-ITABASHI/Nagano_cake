<h1>商品一覧</h1>（全<%= @items.count %>件)
<% @items.each do |item| %>
 <%= link_to public_item_path(item.id) do %>
 <%= image_tag item.get_image(100,100) %>
 <% end %>
 <%= item.name %>
 ￥<%= item.price %>
<% end %>